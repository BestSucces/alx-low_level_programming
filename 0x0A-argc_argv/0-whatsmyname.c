#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * @argc: funtion paramter that counts number of arguments passed
 * @argv: function paramter that prints actual arguments passsed
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n" , argv[0]);
		return (0);

}
